# 사업계획서 자동화 워크플로우

## 트리거
사용자가 "프로젝트 시작", "계속 진행", "사업계획서 진행" 등의 명령 시 자동 시작

---

## 메인 루프

```
FOR 각 섹션 IN SECTION_ORDER:
    1. state.json에서 섹션 상태 확인

    2. IF state == "pending":
        → GOTO 전처리 단계

    3. IF state == "research_needed":
        → [알림] 리서치 필요
        → 대기 (사용자가 "리서치 완료" 알릴 때까지)

    4. IF state == "human_input_needed":
        → [알림] 사용자 입력 필요
        → 대기 (사용자가 "입력 완료" 알릴 때까지)

    5. IF state == "drafted" OR state == "feedback_received":
        → GOTO 평가 루프

    6. IF state == "verified" AND score >= 95:
        → 다음 섹션으로 이동
```

---

## 전처리 단계

```
FUNCTION 전처리(섹션):
    IF needs_research == true AND research NOT IN research_completed:
        → state = "research_needed"
        → [알림] "📋 {섹션명} 리서치 필요"
                 "research/{research_type}.md 파일에 리서치 결과를 복사해주세요"
                 "완료 후 '리서치 완료' 알려주세요"
        → RETURN (대기)

    IF needs_human == true AND inputs/{섹션}_input.md 미작성:
        → state = "human_input_needed"
        → [알림] "✏️ {섹션명} 사용자 입력 필요"
                 "inputs/{섹션}_input.md 파일을 작성해주세요"
                 "완료 후 '{섹션} 입력 완료' 알려주세요"
        → RETURN (대기)

    → 초안 작성
    → state = "drafted"
    → GOTO 평가 루프
```

---

## 평가 루프 (자동 반복)

```
FUNCTION 평가루프(섹션):
    WHILE iteration < 10 AND score < 95:
        1. 현재 버전 평가 요청
        2. 피드백 파싱 (SCORE, WEAKNESSES, PRIORITY_FIX)
        3. scores/{섹션}_feedback_v{n}.txt 저장
        4. state.json 업데이트 (score, iteration)

        IF score >= 95:
            → state = "verified"
            → [알림] "✅ {섹션명} 완료 (점수: {score})"
            → BREAK

        IF iteration >= 10:
            → state = "max_iteration"
            → [알림] "⚠️ {섹션명} 최대 반복 도달 (점수: {score})"
                     "수동 검토 필요"
            → BREAK

        5. 피드백 반영하여 재작성
        6. drafts/{섹션}_v{n+1}.md 저장
        7. iteration += 1

    → 다음 섹션으로 이동
```

---

## 알림 유형

| 코드 | 의미 | 사용자 액션 |
|------|------|-------------|
| 📋 리서치 필요 | 해당 섹션에 시장/경쟁사 데이터 필요 | research/ 폴더에 결과 복사 → "리서치 완료" |
| ✏️ 입력 필요 | 사용자만 알 수 있는 정보 필요 | inputs/ 파일 작성 → "{섹션} 입력 완료" |
| ✅ 섹션 완료 | 95점 이상 달성 | 없음 (자동 진행) |
| ⚠️ 수동 검토 | 10회 반복 후 95점 미달 | 직접 검토 후 "계속 진행" |
| 🔄 진행 중 | 평가/재작성 루프 진행 중 | 없음 (자동 진행) |

---

## 사용자 명령어

| 명령 | 동작 |
|------|------|
| "프로젝트 시작" / "계속 진행" | 메인 루프 시작 |
| "리서치 완료" | research_needed → drafted 전환, 초안 작성 시작 |
| "{섹션} 입력 완료" | human_input_needed → drafted 전환, 초안 작성 시작 |
| "현재 상태" | 전체 섹션 진행률 출력 |
| "{섹션} 다시 작성" | 해당 섹션 iteration 리셋, 재시작 |

---

## 파일 구조

```
projects/ai_court_auction/
├── CLAUDE.md               # ⭐ Claude 행동 규칙 (자동 실행 규칙)
├── WORKFLOW.md             # 이 파일 (프로세스 정의)
├── state.json              # 전체 상태 관리
├── lib/
│   ├── prompts.sh          # ChatGPT 프롬프트 (질문 추출 포함)
│   ├── sections.sh         # 섹션 정의
│   └── ...
├── drafts/
│   ├── section_1_2_v1.md   # 초안
│   ├── section_1_2_v2.md   # 1차 수정
│   └── ...
├── inputs/                 # 사용자 입력 템플릿 (질문+예시)
│   ├── closure_details_input.md  ✅ 생성됨
│   ├── s1_1_input.md             ✅ 생성됨
│   └── s4_1_input.md             ✅ 생성됨
├── research/               # 리서치 결과
│   ├── market_size.md      ✅ 완료
│   ├── competitive.md      📋 템플릿 생성됨 (s3_1용)
│   └── customer_needs.md   📋 템플릿 생성됨 (s3_2용)
└── scores/
    ├── section_s1_2_scores.log
    └── section_s1_2_feedback_v3.txt
```

---

## 리서치 필요 섹션

| 섹션 | 리서치 유형 | 파일명 | 상태 |
|------|-------------|--------|------|
| s1_3 | market_size | research/market_size.md | ✅ 완료 |
| s3_1 | competitive | research/competitive.md | 📋 필요 |
| s3_2 | customer_needs | research/customer_needs.md | 📋 필요 |

---

## 사용자 입력 필요 섹션

| 섹션 | 입력 파일 | 상태 |
|------|-----------|------|
| closure_details | inputs/closure_details_input.md | ✏️ 작성 대기 |
| s1_1 | inputs/s1_1_input.md | ✏️ 작성 대기 |
| s4_1 | inputs/s4_1_input.md | ✏️ 작성 대기 |
